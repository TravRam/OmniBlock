<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cop4331.trav.omniblock" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".StatisticsActivity" />

        <service
            android:name=".StatisticService"
            android:exported="false" />

        <activity
            android:name=".BlacklistRead"
            android:label="@string/title_activity_blacklist_read" >
        </activity>
        <activity
            android:name=".WhitelistRead"
            android:label="@string/title_activity_whitelist_read" >
        </activity>
    </application>

</manifest>
